table.table.table-hover.table-condensed
    thead
        th(ng-repeat="field in recordsPageModel.activeScreen.summary") {{ field.label }}
    tbody
        tr(ng-repeat="record in recordsPageModel.displayData")
            td(ng-repeat="field in record.data" ng-click="drillIntoRecord(record._id)") {{ field.value }}
